UI BEAUTIFICATION: Remove nested borders and enhance tool call styling

PHASE 1: ANALYZE CURRENT STATE
- [x] Identify all elements with nested borders causing harshness
  - tool-block with tool-header and tool-body (nested borders at 718-790)
  - thought-block with thought-header and thought-content (nested borders at 689-716)
  - plan-block with plan-header and plan-item (nested borders at 797-816)
  - message-block with execution-blocks (nested borders at 1709-1884)
  - tool-icon has inner border (line 742)
  - file-path has background that creates visual nesting (line 1857)
  - message-bubble in stream-container (nested visual hierarchy)

- [x] Identify tool call rendering code
  - app.js renderMessageBlock() method handles tool_use blocks (line 230-235)
  - Currently renders JSON as raw escaped text in <pre> tag
  - No parameter interpretation or styled display

- [x] Current CSS issues
  - All block elements have `border: 1px solid var(--border-color)`
  - Headers have `background: var(--bg-tertiary)` + border-top
  - Bodies have separate backgrounds + borders creating visual stack effect
  - Inner elements (tool-icon, file-path) add secondary borders

PHASE 2: REMOVE HARSH NESTED BORDERS
- [x] Update styles.css to remove inner element borders
  - [x] Remove border from .tool-icon (line 742)
  - [x] Remove border-top from .tool-body (line 790)
  - [x] Update .file-path styling (line 1857)
  - [x] Consolidate with background-only visual hierarchy

- [x] Apply to related nested structures
  - [x] .block-tool-use and nested .tool-input
  - [x] .block-file-op and nested .file-path, .file-content
  - [x] .block-tool-result and nested structures

PHASE 3: ENHANCE TOOL CALL PARAMETER RENDERING
- [x] Update app.js renderMessageBlock() for tool_use blocks
  - [x] Create renderParameters() function
  - [x] Parse block.input object into styled HTML
  - [x] Handle strings, objects, arrays, paths, etc.

- [x] Update tool_result rendering
  - [x] Format result JSON nicely
  - [x] Use appropriate styling based on content type

PHASE 4: CREATE VISUAL CONSISTENCY
- [x] Ensure all parameter displays use consistent styling
- [x] Verify styling in light and dark modes
- [x] Test with various parameter types (strings, paths, numbers, objects, arrays)

PHASE 5: TESTING & VERIFICATION
- [x] Created test HTML file with multiple parameter types
- [x] Verified tool calls display cleanly without harsh borders
- [x] Tested dark mode - styling consistent and readable
- [x] Verified parameter rendering for:
  - [x] File paths (monospace, blue color)
  - [x] String values (quoted properly)
  - [x] Numbers (colored blue)
  - [x] Object keys (blue, proper indentation)
  - [x] Nested structures (clean, readable format)
